<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <nav class="p-4 h-[4rem] flex justify-between items-center">
        <div>
            <a class="text-2xl text-blue-400 font-semibold" href="">Scatch</a>
        </div>
        <% var logged=typeof logged !=="undefined" ? logged : true %>
            <% if(logged){ %>
                <div class="flex gap-5">
                    <a class="" href="">Shop</a>
                    <a href="/cart">Cart</a>
                    <a href="">My Account</a>
                    <a class="text-red-600" href="/users/logout">Logout</a>
                </div>
                <% } %>
    </nav>
</body>

</html>