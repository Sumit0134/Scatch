<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Scatch - A Premium Bag Shop</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <% if(error.length>0){ %>
        <div class="absolute top-5 p-3 rounded-lg bg-red-500 text-white left-1/2 -translate-x-1/2 -translate-y-1/2">
            <span class="inline-block text-sm mt-1 mb-1">
                <%= error %>
            </span>
        </div>
    <% } %>

    <div class="w-full min-h-screen flex px-20">
        <div class="w-1/2 flex items-center justify-center min-h-screen">
            <div class="w-full px-32">
                <h3 class="text-4xl">Welcome to <span class="text-blue-400 font-semibold">Scatch</span></h3>
                <h4 class="text-2xl mb-5">Create your Account</h4>
                <form autocomplete="off" action="/users/register" method="post">
                    <input class="bg-zinc-100 mb-3 border-[1px] border-zinc-200 px-3 py-2 block rounded-lg w-full outline-none" type="text" placeholder="Full Name" name="fullName">
                    <input class="bg-zinc-100 mb-3 border-[1px] border-zinc-200 px-3 py-2 block rounded-lg w-full outline-none" type="email" placeholder="Email" name="email">
                    <input class="bg-zinc-100 mb-3 border-[1px] border-zinc-200 px-3 py-2 block rounded-lg w-full outline-none" type="password" placeholder="Password" name="password">
                    <input class="rounded-full text-white mt-2 px-4 py-2 cursor-pointer bg-blue-500" type="submit" value="Create">
                </form>
            </div>
        </div>

        <div class="w-1/2 flex items-center justify-center min-h-screen">
            <div class="w-full px-32">
                <h4 class="text-2xl mb-5">Login your Account</h4>
                <form autocomplete="off" action="/users/login" method="post">
                    <input class="bg-zinc-100 block px-3 py-2 mb-3 border-[1px] border-zinc-200 w-full rounded-lg outline-none" type="email" name="email" placeholder="Email">
                    <input class="bg-zinc-100 block px-3 py-2 mb-3 border-[1px] border-zinc-200 w-full rounded-lg outline-none" type="password" name="password" placeholder="Password">
                    <input class="rounded-full text-white mt-2 px-4 py-2 cursor-pointer bg-blue-500" type="submit" value="Login">
                </form>
            </div>
        </div>
    </div>
</body>
</html>